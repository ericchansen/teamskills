### 2026-03-02T20:01:57Z: User directive — Exhaustive verification before claiming done
**By:** Eric Hansen (via Copilot)
**What:** Never, ever tell Eric you're done until you have double or even triple checked. Exhaust your own testing abilities before telling him something works. A health check or HTTP 200 is NOT verification — you must load the actual page in a browser, verify real data displays, check for duplicates, take screenshots, and confirm the full user experience works end-to-end.
**Why:** User request — captured for team memory. Triggered by coordinator claiming production was working based only on a health check and HTTP 200 status code, without loading the site in a browser. When actually checked, the data had duplicate users from the CSV import. This directive prevents false claims of success.
**Context:** The specific failure was: pivot CSV importer created duplicate users (brandon.babcock@placeholder.local vs existing Brandon Babcock with real email). The coordinator said "production is live and working" based on `GET /health` returning ok and `GET /` returning 200. When actually loaded in a browser, duplicates were visible. The lesson: always verify the actual user experience, not just infrastructure signals.
